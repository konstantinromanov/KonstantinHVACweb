@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<nav class="navbar bg-dark navbar-dark navbar-expand-lg fixed-top">

    <div class="container">

        <a asp-controller="Home" asp-action="Index" class="navbar-brand"><img src="~/content/images/avk_dizains_logo.svg" alt="HVAC" title="HVAC" /></a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item"><a class="nav-link @(ViewBag.Current == "Index" ? "active" : "")" href="@Url.Action("Index","Home")" target="_self">Sākums</a></li>
                <li class="nav-item"><a class="nav-link @(ViewBag.Current == "About" ? "active" : "")" href="@Url.Action("About","Home")" target="_self">Par mums</a></li>
                <li class="nav-item"><a class="nav-link @(ViewBag.Current == "Services" ? "active" : "")" href="@Url.Action("Services","Home")" target="_self">Pakalpojumi</a></li>
                <li class="nav-item"><a class="nav-link @(ViewBag.Current == "Projects" ? "active" : "")" href="@Url.Action("Projects","Home")" target="_self">Projekti</a></li>
                <li class="nav-item"><a class="nav-link @(ViewBag.Current == "Contact" ? "active" : "")" href="@Url.Action("Contact","Home")" target="_self">Kontakti</a></li>
            </ul>

            <ul class="navbar-nav right-nav-container">
                <div class="dropdown show">
                    <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="false" aria-expanded="false">
                        LV
                    </a>

                    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                        <a class="nav-link" href="#">EN</a>

                    </div>
                </div>
            </ul>

        </div>

    </div>
</nav>